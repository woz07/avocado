package avocado.core;

import java.util.HashMap;

/**
 * Tokenizer.java
 * This is the class that tokenizes a given string
 */

public class Tokenizer {
    /**
     * This function tokenizes a string and returns a hashmap with its tokens as values
     * and its words as the keys
     * @param line The input line to tokenize
     * @return The hashmap containing the newly tokenized data
     */
    public static HashMap<String, Tokens> tokenize(String line) {
        return null;
    }
}
